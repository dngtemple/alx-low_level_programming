#include "main.h"

/**
 * _abs - checking the absolute value
 * @n: number to be checked
 *
 * Return: the absolute value of the integer
 */

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}

